const rp = require('request-promise');
module.exports.description = {
    name:"skip",
    description:"Skips to the next queued song. If none is queued, the bot leaves the voice channel",
    usage: "`!skip`"
}