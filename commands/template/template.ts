import * as discord from 'discord.js';
import {DatabaseHelper} from '../../database-helper';
export async function template(client: discord.Client, dbh:DatabaseHelper, msg: discord.Message, args: String[]) {
    
}   